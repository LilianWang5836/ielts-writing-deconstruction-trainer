/**
 * Coach Agent 各 Step 的 prompt 模板
 *
 * 各 Step 的 coach prompt 已实现在 coach-agent.ts 的 buildCoachPrompt 中
 * 此文件供后续需要集中管理或自定义不同 Step 的 prompt 时使用
 */

export { buildCoachPrompt } from '../coach/coach-agent';
