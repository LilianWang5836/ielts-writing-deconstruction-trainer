/**
 * Intent Agent prompt 模板 + Gemini response_schema 定义
 *
 * prompt 已实现在 intent-agent.ts 的 buildIntentPrompt 中
 * 此文件供后续需要集中管理 prompt 时使用
 */

export { buildIntentPrompt } from '../coach/intent-agent';
